# Kalau jalankan npm run dev dari PC kamu sendiri (bukan di LAN yang sama
# dengan VM), pakai IP publik + port NAT Webserver VM:
VITE_API_BASE_URL=http://103.139.163.150:3311/api

# Kalau frontend-nya jalan di jaringan yang sama dengan VM (LAN 192.168.38.0/24),
# bisa langsung ke Webserver VM:
# VITE_API_BASE_URL=http://192.168.38.5:8000/api
